return { -- use gc (line comment) and gb (block comment) in visual mode to comment out lines
    {
        'numToStr/Comment.nvim',
        opts = {
            -- add any options here
        },
        lazy = false,
    }
}
